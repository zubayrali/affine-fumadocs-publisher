# Contributing

This project is in its `0.1.0` foundation phase. Please open an issue before
large feature work, especially around AFFiNE export semantics, Edgeless, Bases,
and authentication.

Run the checks before proposing a change:

```bash
pnpm test
pnpm check
```

Fixtures must never contain private AFFiNE workspace data or real credentials.
