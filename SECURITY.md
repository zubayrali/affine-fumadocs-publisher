# Security policy

Do not report credentials, workspace content, browser cookies, or private
document URLs in public issues. For a suspected vulnerability, use GitHub's
private security advisory flow for this repository once it is enabled.

The publisher is designed to keep AFFiNE credentials in the local publishing
environment. Never expose its bridge endpoint publicly without HTTPS and
separate authentication.
